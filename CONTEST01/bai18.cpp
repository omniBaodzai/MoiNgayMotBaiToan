/*
=====================================================================================================================================================================
bài 18: Đề bài:
Cho 2 số nguyên a, b bạn hãy hoán đổi giá trị 2 số a và b sau đó tính giá trị biểu
thức 128 * a + 97 * b + 1000 in ra màn hình.
Để hoán đổi giá trị a, b bạn có thể dùng swap(a, b) hoặc dùng biến tạm như sau:
int tmp = a; a = b; b = tmp;
=====================================================================================================================================================================
- Đầu vào:
Dòng duy nhất gồm 2 số a, b
=====================================================================================================================================================================
- Ràng buộc: 
0 <= a, b <= 10^9
=====================================================================================================================================================================
- Đầu ra:
In ra a b sau khi hoán đổi giá trị
=====================================================================================================================================================================
- Input 01:
12 16
=====================================================================================================================================================================
- Output 01:
4212
=====================================================================================================================================================================
*/
#include <iostream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int a, b;
    cin >> a >> b;
  
    swap(a,b);
    /*int temp = a;
    a = b;
    b = temp;*/
  
    //cout << a << " " << b << endl;
    cout << 128 * 1ll * a + 97 * 1ll * b + 1000 << endl;
  
    return 0;
}

/*
#include <iostream>

using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;

    // Hoán đổi giá trị a và b
    swap(a, b);

    // Tính giá trị biểu thức
    cout << 128LL * a + 97LL * b + 1000 << endl;

    return 0;
}

*/